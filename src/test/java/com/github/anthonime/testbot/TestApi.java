package com.github.anthonime.testbot;

import org.junit.Test;

/**
 * Created by schio on 9/11/2017.
 */
public class TestApi {

    @Test
    public void name() throws Exception {
        //given an element, i can retrieve the parent element
        ///the parent is visible
        //the parent is present & visible


        //i can easily find an input that match name/value/type/label/placeholder

        //patterns: div/ input + label

        //first find all inputs => inputs (with text) , textarea
        //what about editable contents ?
        //first find all buttons + a + input button => clickable, role="button", role="link", submit
        //id contains "next"
        //

        //deduce a kind of structure:
        //

        //

    }
}
